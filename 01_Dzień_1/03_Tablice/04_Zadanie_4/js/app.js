function printTable(array){
    array.forEach( function (element) {
            console.log(element);
        }
    )
}

const arrayToPrint = ["Adam", "Marek", "Iwona", 49, 98];
printTable(arrayToPrint);



